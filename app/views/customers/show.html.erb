<h1><%= @customer.name%></h1>

Products Bought:
<ul>
<% @customer.products.each do |product| %>
    <li><%= product.name %> of type <%= product.type_of_product %></li>
<% end %>

<!-- if we have duplicates, just put how many of that we've bought, rather than list twice -->